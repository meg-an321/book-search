const User = require('./User');
const Book = require('./Book'); //importing the Book model 

module.exports = { User, Book }; //exporting the User and Book models which will be used in the server.js file